#!/usr/bin/make -f
# See debhelper(7) (uncomment to enable)
# output every command that modifies files on the build system.
#export DH_VERBOSE = 1

# see FEATURE AREAS in dpkg-buildflags(1)
#export DEB_BUILD_MAINT_OPTIONS = hardening=+all

# see ENVIRONMENT in dpkg-buildflags(1)
# package maintainers to append CFLAGS
export DEB_CFLAGS_MAINT_APPEND  = -fPIC
# package maintainers to append LDFLAGS
# Note: --no-as-needed is critical: modules are loaded at runtime via QLibrary (dlopen),
# so transitive dependencies must not be stripped at link time.
export DEB_LDFLAGS_MAINT_APPEND = -Wl,--no-as-needed -fPIC


%:
	dh $@ --buildsystem=cmake

override_dh_auto_configure:
	if [ -d .git ]; then git submodule update --init --recursive; fi
	test -f libs/stellarsolver/CMakeLists.txt || \
		{ echo "ERROR: libs/stellarsolver not populated. Add nest directive to Launchpad recipe."; exit 1; }
	dh_auto_configure
